{
        http_port 8080
        auto_https off
}

localhost:8080 {
        root * {$SITE_ROOT}
        encode gzip
        file_server
}

http://fcunynet.fly.dev {
        redir https://fcuny.net
}

http://fcuny.net {
        root * {$SITE_ROOT}
        encode gzip
        file_server
}
